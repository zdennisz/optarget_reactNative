import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LensTargetInput = (props) => {
	return <Text style={styles.conatiner}>Lens Target Input Screen</Text>;
};

const styles = StyleSheet.create({
	conatiner: {
		flex: 1,
		justifyContent: "center",
		alignItems: "center",
	},
});

export default LensTargetInput;
